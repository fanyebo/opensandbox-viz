[project]
name = "opensandbox-viz"
version = "0.1.0"
description = "OpenSandbox visualization dashboard"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.35",
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "pyinstaller",
]

[project.scripts]
viz = "app:main"
